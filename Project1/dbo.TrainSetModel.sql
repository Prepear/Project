CREATE TABLE [dbo].[TrainSetModel] (
    [train_set_model_id] INT IDENTITY (1, 1) NOT NULL,
    [train_set_model_name] NVARCHAR(50) NULL, 
    PRIMARY KEY CLUSTERED ([train_set_model_id] ASC)
);

